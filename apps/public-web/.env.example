# Relative /api uses the Vite dev proxy (vite.config.ts) so session cookies stay same-origin.
# For a built preview without the proxy, point at the API host explicitly instead.
PUBLIC_API_BASE_URL=/api
